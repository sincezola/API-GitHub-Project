import React from 'react';

const Error = () => {
  return (
    <div>
      <p>Usuário não encontrado!</p> {/* Colocando o erro hardcoded pois aqui só tem esse tipo de erro, se quiser implementar, é válido. */}
    </div>
  );
};

export default Error;